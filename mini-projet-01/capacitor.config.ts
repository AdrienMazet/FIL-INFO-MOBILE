import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mini-projet-01',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
